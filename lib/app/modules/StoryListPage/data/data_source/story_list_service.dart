import 'package:http/http.dart' as http;

import '../../../../common/fake_data.dart';

class StoriesListApiService {
  Future<dynamic> fetchStories() async {
    // Making fske API call to fetch the list of stories
    // final response = await http.get(Uri.parse(url),);

    

    return fake_data;
  }
}
